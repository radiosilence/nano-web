name: Push Package amd64

permissions:
  contents: read

on:
  push:
    tags:
      - v*
jobs:
  publish:
    uses: radiosilence/nano-web/.github/workflows/push-package.yml@main
    with:
      arch: amd64
    secrets:
      OPS_KEY: ${{ secrets.OPS_KEY }}
